//js code

// value: 基本数据类型 & 引用数据类型
// Number, String, Boolean, Undefined, null
// Array

// if (条件) { code ...} else if (条件) { code ...} else { code ... }
// switch (value1) {
//      case value:
//          code..;
//          break;
//      case value:
//          code...;
//          break;
//      default:
//          code...;
// }   value1会和各个value进行比较,为真执行对应后面的代码

// 循环:
//     for(初始化条件;判断条件;更新条件变量){ cdde...; }    条件变量是在代码块执行后才更新
//     while(条件){ code...; }
//     do { code...; } while(条件);
//     break && continue

//比较运算符:
//  === 和 !==  (等同于&不等同于, 既比较值,又比较类型, 推荐使用)
//  ==  和  ！= (只比较值)
//  >= <=

//逻辑运算符:
//  && || !

// 三元运算符:
//    (条件)?条件真执行代码块:条件假执行代码块;     等价 if (条件) { code...; } else { code; }

// function Name(arg1, arg2 ...) {
//      code...;
//      return value;
//  }

// object(包含属性和方法):
//     var person = {
//         name: 'bobo',
//         age: 18,
//         sex: '男',
//         fav: function(){
//              alert("dfadf");
//         }
//     }
// 用.对persion对象中的属性进行调用

