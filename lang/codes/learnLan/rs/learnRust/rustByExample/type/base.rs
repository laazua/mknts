// rust基础类型

fn main() {
    // 有符号整型 i8, i16, i32, i64, i128 and isize (pointer size)
    // 无符号整型 u8, u16, u32, u64, u128 and usize (pointer size)
    // 浮点型 f32, f64
    // 字符型 char Unicode标量4字节
    // 布尔型 true, false
    // ()型
    let logical: bool = true;

    let a_float: f64 = 1.0;  
    let an_integer   = 5i32;
}
