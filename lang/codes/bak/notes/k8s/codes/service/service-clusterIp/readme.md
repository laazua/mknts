##### ClusterIp

* **ClusterIp**
```
  - k8s内部服务之间的流量互通,使用此类型
```

```
  - kubectl apply -f mysql-pod-example.yml
  - kubectl apply -f mysql-svc-example.yml
  - kubectl apply -f deployment-example.yml
  - kubectl apply -f svc-example.yml
```