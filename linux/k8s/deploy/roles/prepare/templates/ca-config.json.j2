{
  "signing": {
    "default": {
       "expiry": "{{ expired_time }}"
    },
    "profiles": {
      "kubernetes": {
        "usages": [
          "signing",
          "key encipherment",
          "server auth",
          "client auth"
        ],
        "expiry": "{{ expired_time }}"
      }
    }
  }
}